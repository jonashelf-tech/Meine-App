# Kognitiv — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Kognitives Trainings-Tool (Tab 16) mit 3 Modulen (Alertness, Zahlensuche, Arbeitsgedächtnis), ultra-minimalem Übungs-UI, Session-Archiv und Dashboard.

**Architecture:** Standalone tool mit internem Zwei-Tab-System (Module | Dashboard). Navigation via `nav`-State-Objekt `{ screen, ...payload }`. Datenlogik in `sessionStore.js` getrennt vom UI. Exercises sind eigenständige Komponenten die `onDone(session)` / `onAbort()` aufrufen.

**Tech Stack:** React 18, CSS Modules, Zustand (`useAppStore` für `days`/`setDays`), `sv`/`lv`/`SK` aus `storage/index.js`

---

## File Structure

**New files:**
```
src/features/tools/kognitiv/
  TabKognitiv.jsx              root + nav state machine + tab switcher
  TabKognitiv.module.css
  sessionStore.js              pure data functions (no React)
  ModuleList.jsx               "Module"-Tab: Modul-Karten
  ModuleList.module.css
  Briefing.jsx                 Briefing-Screen vor Übung
  Briefing.module.css
  DoneToday.jsx                "Heute schon erledigt"-Screen
  DoneToday.module.css
  Results.jsx                  Ergebnis-Screen nach Übung
  Results.module.css
  Dashboard.jsx                "Dashboard"-Tab
  Dashboard.module.css
  ModuleDetail.jsx             Modul-Historie aus Dashboard
  ModuleDetail.module.css
  SessionDetail.jsx            Einzel-Session Tiefenanalyse
  SessionDetail.module.css
  exercises/
    AlertnessExercise.jsx
    AlertnessExercise.module.css
    ZahlensucheExercise.jsx
    ZahlensucheExercise.module.css
    GedaechtnisExercise.jsx
    GedaechtnisExercise.module.css
```

**Modified files:**
```
src/features/tools/toolRegistry.jsx    add kognitiv entry + SVG icon
src/features/tools/toolTabs.js         add kognitiv: 16
src/storage/index.js                   add SK.kognitiv
src/App.jsx                            import + route TabKognitiv
```

---

## Module Config (referenced in multiple tasks)

```js
// MODULE_CONFIG — used in ModuleList, Briefing, Results, Dashboard
export const MODULE_CONFIG = {
  alertness: {
    id: 'alertness',
    name: 'Alertness',
    desc: 'Tippe so schnell wie möglich wenn der Stimulus erscheint.',
    duration: 'ca. 3 Minuten',
    measured: ['Reaktionszeit (ms)', 'Fehler (Falschtippen)', 'Auslasser (verpasst)'],
    notMeasured: ['Intelligenz', 'Ablenkungen außerhalb des Screens'],
    mainMetricLabel: 'Ø Reaktionszeit',
    mainMetricUnit: 'ms',
    variants: ['Ohne Ton', 'Mit Warnton'],
    defaultVariant: 'Ohne Ton',
  },
  zahlensuche: {
    id: 'zahlensuche',
    name: 'Zahlensuche',
    desc: '25 Felder mit Zahlen 01–25. Tippe sie in Reihenfolge so schnell wie möglich.',
    duration: 'ca. 2–4 Minuten',
    measured: ['Gesamtzeit (s)', 'Fehler', 'Zeit pro Zahl'],
    notMeasured: ['Rechenkenntnisse', 'Ablenkungen außerhalb des Screens'],
    mainMetricLabel: 'Gesamtzeit',
    mainMetricUnit: 's',
    variants: ['Normal', 'Schwer', 'Rückwärts'],
    defaultVariant: 'Normal',
  },
  gedaechtnis: {
    id: 'gedaechtnis',
    name: 'Arbeitsgedächtnis',
    desc: '12 Kreise leuchten in Reihenfolge auf — merke dir die Sequenz und reproduziere sie.',
    duration: 'ca. 5 Minuten',
    measured: ['Korrekte Sequenzen (max. 8)', 'Fehler pro Runde', 'Max. Sequenzlänge'],
    notMeasured: ['Gesamtintelligenz', 'Tagesform außerhalb der Übung'],
    mainMetricLabel: 'Korrekte',
    mainMetricUnit: '/ 8',
    variants: ['Normal', 'Schwer'],
    defaultVariant: 'Normal',
  },
}

export const MODULE_ORDER = ['alertness', 'zahlensuche', 'gedaechtnis']
export const PHASE2_MODULES = ['vigilanz', 'selektion', 'geteilt']
```

---

## Task 1: Tool-Scaffolding

**Files:**
- Modify: `src/features/tools/toolTabs.js`
- Modify: `src/storage/index.js`
- Modify: `src/features/tools/toolRegistry.jsx`
- Modify: `src/App.jsx`
- Create: `src/features/tools/kognitiv/TabKognitiv.jsx`
- Create: `src/features/tools/kognitiv/TabKognitiv.module.css`

- [ ] **1.1 toolTabs.js — add kognitiv: 16**

```js
// src/features/tools/toolTabs.js
export const TOOL_TAB = {
  geburtstage:  4,
  timer:        5,
  rezepte:      6,
  pizza:        7,
  elvi:         8,
  gewicht:      9,
  erfolge:      10,
  rad:          11,
  reminder:     12,
  haushalt:     13,
  wasjetzt:     14,
  klaeren:      15,
  kognitiv:     16,   // ← neu
}
```

- [ ] **1.2 storage/index.js — add SK.kognitiv**

Nach der letzten SK-Zeile (z.B. nach `erfolgeTracking`) ergänzen:
```js
  kognitiv: `${PREFIX}kognitiv_sessions`,
```

- [ ] **1.3 toolRegistry.jsx — Icon + Eintrag**

In `ICONS` ergänzen:
```js
kognitiv: { el: (s) => (
  <svg width={s} height={s} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
    <path d="M12 2a10 10 0 0 1 10 10c0 5.52-4.48 10-10 10S2 17.52 2 12"/>
    <circle cx="12" cy="12" r="3"/>
    <path d="M12 9V5M15 12h4"/>
  </svg>
)},
```

In `TOOL_REGISTRY` ergänzen (nach klaeren):
```js
{
  id:          'kognitiv',
  name:        'Kognitiv',
  icon:        '🧠',
  color:       '#8B5CF6',
  description: 'Kognitives Training: Reaktion, Aufmerksamkeit, Gedächtnis',
  standalone:  true,
  integrated:  true,
},
```

- [ ] **1.4 App.jsx — import + route**

Import hinzufügen (nach TabKlaeren):
```js
import TabKognitiv from './features/tools/kognitiv/TabKognitiv'
```

Route hinzufügen (nach der klaeren-Zeile):
```jsx
{currentTab === TOOL_TAB.kognitiv && <TabKognitiv onBack={goBack} />}
```

- [ ] **1.5 TabKognitiv.jsx erstellen — Shell**

```jsx
// src/features/tools/kognitiv/TabKognitiv.jsx
import { useState } from 'react'
import ToolHeader from '../../../components/ToolHeader/ToolHeader'
import { TOOL_TAB } from '../toolTabs'
import { useAppStore } from '../../../store'
import { ToolIcon } from '../toolRegistry'
import s from './TabKognitiv.module.css'

// Nav screens: 'modules' | 'dashboard' | 'briefing' | 'done-today' |
//              'exercise' | 'results' | 'module-detail' | 'session-detail'

export default function TabKognitiv({ onBack }) {
  const [tab, setTab] = useState('modules')   // 'modules' | 'dashboard'
  const [nav, setNav] = useState(null)        // null = show tabs; object = overlay screen

  const goBack = () => setNav(null)

  if (nav?.screen === 'briefing') {
    return <div className={s.placeholder}>Briefing — {nav.moduleId}</div>
  }
  if (nav?.screen === 'done-today') {
    return <div className={s.placeholder}>DoneToday — {nav.moduleId}</div>
  }
  if (nav?.screen === 'exercise') {
    return <div className={s.placeholder}>Exercise — {nav.moduleId}</div>
  }
  if (nav?.screen === 'results') {
    return <div className={s.placeholder}>Results</div>
  }
  if (nav?.screen === 'module-detail') {
    return <div className={s.placeholder}>ModuleDetail — {nav.moduleId}</div>
  }
  if (nav?.screen === 'session-detail') {
    return <div className={s.placeholder}>SessionDetail</div>
  }

  return (
    <div className={s.root}>
      <ToolHeader onBack={onBack} icon={<ToolIcon id="kognitiv" size={20} />} eyebrow="Tool" title="Kognitiv" />
      <div className={s.tabBar}>
        <button className={[s.tabBtn, tab === 'modules' ? s.tabOn : ''].join(' ')} onClick={() => setTab('modules')}>Module</button>
        <button className={[s.tabBtn, tab === 'dashboard' ? s.tabOn : ''].join(' ')} onClick={() => setTab('dashboard')}>Dashboard</button>
      </div>
      <div className={s.content}>
        {tab === 'modules'
          ? <div className={s.placeholder}>ModuleList kommt in Task 3</div>
          : <div className={s.placeholder}>Dashboard kommt in Task 10</div>
        }
      </div>
    </div>
  )
}
```

- [ ] **1.6 TabKognitiv.module.css erstellen**

```css
.root { display: flex; flex-direction: column; height: 100%; background: var(--bg); }
.tabBar { display: flex; border-bottom: 1px solid var(--border); padding: 0 16px; flex-shrink: 0; }
.tabBtn { flex: 1; background: none; border: none; border-bottom: 2px solid transparent; color: var(--text-dim); font-family: 'Outfit', sans-serif; font-size: 0.78rem; font-weight: 600; padding: 10px 0; cursor: pointer; transition: all 0.15s; }
.tabOn { color: var(--primary); border-bottom-color: var(--primary); }
.content { flex: 1; overflow-y: auto; }
.placeholder { padding: 24px; color: var(--text-dim); font-size: 0.8rem; }
```

- [ ] **1.7 App starten und kognitiv-Tool aktivieren, Tab öffnen**

```
npm run dev
```

Im Browser: Tools → "+ Alle Tools" → Kognitiv aktivieren → Kognitiv-Tab öffnet → Placeholder sichtbar.

- [ ] **1.8 Commit**

```bash
git add src/features/tools/toolTabs.js src/storage/index.js src/features/tools/toolRegistry.jsx src/App.jsx src/features/tools/kognitiv/
git commit -m "feat(kognitiv): tool scaffolding, routing, empty shell"
```

---

## Task 2: sessionStore.js

**Files:**
- Create: `src/features/tools/kognitiv/sessionStore.js`

- [ ] **2.1 sessionStore.js erstellen**

```js
// src/features/tools/kognitiv/sessionStore.js
import { sv, lv, SK } from '../../../storage'

const genId = () =>
  crypto.randomUUID?.() ?? `${Date.now().toString(36)}-${Math.random().toString(36).slice(2)}`

export function loadSessions() {
  return lv(SK.kognitiv, [])
}

export function saveSession(session) {
  const sessions = loadSessions()
  sv(SK.kognitiv, [...sessions, session])
}

export function createSession({ moduleId, variant, startedAt, duration, score, mainMetric, taps }) {
  return {
    id:        genId(),
    moduleId,
    variant,
    date:      startedAt.slice(0, 10),   // "2026-05-28"
    startedAt,
    duration,
    score,
    mainMetric,
    taps,
  }
}

export function isDoneToday(moduleId) {
  const today = new Date().toISOString().slice(0, 10)
  return loadSessions().some(s => s.moduleId === moduleId && s.date === today)
}

export function getLastSession(moduleId) {
  const all = loadSessions().filter(s => s.moduleId === moduleId)
  return all.length > 0 ? all[all.length - 1] : null
}

export function getModuleSessions(moduleId) {
  return loadSessions().filter(s => s.moduleId === moduleId)
}

export function getDelta(moduleId, currentMetric) {
  // returns positive number = improvement (lower time or higher score)
  const last = getLastSession(moduleId)
  if (!last) return null
  return last.mainMetric - currentMetric   // positive = got faster/better
}

export function getModuleStats(moduleId) {
  const sessions = getModuleSessions(moduleId)
  if (sessions.length === 0) return null
  const metrics = sessions.map(s => s.mainMetric)
  const first = metrics[0]
  const best  = Math.min(...metrics)
  const latest = metrics[metrics.length - 1]
  return {
    sessions: sessions.length,
    best,
    latest,
    improvement: first - latest,    // positive = got faster/better
    last7: sessions.slice(-7),
  }
}

export function getWeeklyCount() {
  const weekAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString().slice(0, 10)
  return loadSessions().filter(s => s.date >= weekAgo).length
}
```

- [ ] **2.2 Commit**

```bash
git add src/features/tools/kognitiv/sessionStore.js
git commit -m "feat(kognitiv): sessionStore — create/load/query sessions"
```

---

## Task 3: ModuleList

**Files:**
- Create: `src/features/tools/kognitiv/ModuleList.jsx`
- Create: `src/features/tools/kognitiv/ModuleList.module.css`
- Modify: `src/features/tools/kognitiv/TabKognitiv.jsx`

- [ ] **3.1 ModuleList.jsx erstellen**

```jsx
// src/features/tools/kognitiv/ModuleList.jsx
import { ToolIcon } from '../toolRegistry'
import { MODULE_CONFIG, MODULE_ORDER, PHASE2_MODULES } from './moduleConfig'
import { isDoneToday, getLastSession, getModuleStats } from './sessionStore'
import s from './ModuleList.module.css'

export default function ModuleList({ onSelectModule }) {
  return (
    <div className={s.list}>
      {MODULE_ORDER.map(id => {
        const m = MODULE_CONFIG[id]
        const done  = isDoneToday(id)
        const stats = getModuleStats(id)
        const last  = getLastSession(id)
        return (
          <button key={id} className={s.card} onClick={() => onSelectModule(id)}>
            <div className={s.iconWrap}>
              <ToolIcon id="kognitiv" size={18} />
            </div>
            <div className={s.info}>
              <div className={s.name}>{m.name}</div>
              <div className={s.desc}>{m.desc.split('.')[0]}.</div>
            </div>
            <div className={s.right}>
              {done && <div className={s.donePill}>✓ heute</div>}
              {!done && last && (
                <div className={s.lastVal}>
                  {last.mainMetric}{m.mainMetricUnit}
                </div>
              )}
              {stats && (
                <div className={s.miniChart}>
                  {stats.last7.map((sess, i) => (
                    <div
                      key={i}
                      className={[s.miniBar, i === stats.last7.length - 1 ? s.miniBarLast : ''].join(' ')}
                      style={{ height: `${Math.max(20, Math.min(100, (stats.best / sess.mainMetric) * 100))}%` }}
                    />
                  ))}
                </div>
              )}
            </div>
          </button>
        )
      })}

      <div className={s.phase2Header}>Phase 2 — kommt bald</div>
      {PHASE2_MODULES.map(id => (
        <div key={id} className={[s.card, s.cardDisabled].join(' ')}>
          <div className={s.iconWrap}><ToolIcon id="kognitiv" size={18} /></div>
          <div className={s.info}>
            <div className={s.name} style={{ textTransform: 'capitalize' }}>{id}</div>
            <div className={s.desc}>In Entwicklung</div>
          </div>
          <div className={s.right}><div className={s.soonPill}>bald</div></div>
        </div>
      ))}
    </div>
  )
}
```

- [ ] **3.2 moduleConfig.js erstellen** (aus Task 1 MODULE_CONFIG auslagern)

```js
// src/features/tools/kognitiv/moduleConfig.js
export const MODULE_CONFIG = {
  alertness: {
    id: 'alertness',
    name: 'Alertness',
    desc: 'Tippe so schnell wie möglich wenn der Kreis erscheint.',
    duration: 'ca. 3 Minuten',
    measured: ['Reaktionszeit (ms)', 'Fehler (Falschtippen)', 'Auslasser (verpasst)'],
    notMeasured: ['Intelligenz', 'Ablenkungen außerhalb des Screens'],
    mainMetricLabel: 'Ø Reaktionszeit',
    mainMetricUnit: 'ms',
    variants: ['Ohne Ton', 'Mit Warnton'],
    defaultVariant: 'Ohne Ton',
  },
  zahlensuche: {
    id: 'zahlensuche',
    name: 'Zahlensuche',
    desc: '25 Felder mit Zahlen 01–25. Tippe sie in Reihenfolge so schnell wie möglich.',
    duration: 'ca. 2–4 Minuten',
    measured: ['Gesamtzeit (s)', 'Fehler', 'Zeit pro Zahl'],
    notMeasured: ['Rechenkenntnisse', 'Ablenkungen außerhalb des Screens'],
    mainMetricLabel: 'Gesamtzeit',
    mainMetricUnit: 's',
    variants: ['Normal', 'Schwer', 'Rückwärts'],
    defaultVariant: 'Normal',
  },
  gedaechtnis: {
    id: 'gedaechtnis',
    name: 'Arbeitsgedächtnis',
    desc: '12 Kreise leuchten nacheinander auf — merke dir die Sequenz.',
    duration: 'ca. 5 Minuten',
    measured: ['Korrekte Sequenzen (max. 8)', 'Fehler pro Runde', 'Max. Sequenzlänge'],
    notMeasured: ['Gesamtintelligenz', 'Tagesform außerhalb der Übung'],
    mainMetricLabel: 'Korrekte',
    mainMetricUnit: '/ 8',
    variants: ['Normal', 'Schwer'],
    defaultVariant: 'Normal',
  },
}

export const MODULE_ORDER = ['alertness', 'zahlensuche', 'gedaechtnis']
export const PHASE2_MODULES = ['vigilanz', 'selektion', 'geteilt']
```

- [ ] **3.3 ModuleList.module.css erstellen**

```css
.list { padding: 12px; display: flex; flex-direction: column; gap: 8px; }
.card { width: 100%; background: var(--surface); border: 1px solid var(--border); border-radius: var(--r); padding: 12px 14px; display: flex; align-items: center; gap: 12px; cursor: pointer; text-align: left; transition: background 0.15s; }
.card:active { background: rgba(255,255,255,0.08); }
.cardDisabled { opacity: 0.35; cursor: default; pointer-events: none; }
.iconWrap { width: 38px; height: 38px; border-radius: var(--r-sm); background: rgba(139,92,246,0.12); display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: var(--primary); }
.info { flex: 1; min-width: 0; }
.name { font-size: 0.88rem; font-weight: 600; color: var(--text); }
.desc { font-size: 0.68rem; color: var(--text-dim); margin-top: 2px; }
.right { display: flex; flex-direction: column; align-items: flex-end; gap: 4px; flex-shrink: 0; }
.donePill { font-size: 0.62rem; font-weight: 600; color: var(--emerald); background: rgba(16,185,129,0.1); border: 1px solid rgba(16,185,129,0.2); border-radius: 20px; padding: 2px 8px; }
.soonPill { font-size: 0.62rem; font-weight: 600; color: var(--text-dim); background: var(--surface); border: 1px solid var(--border); border-radius: 20px; padding: 2px 8px; }
.lastVal { font-family: 'Orbitron', sans-serif; font-size: 0.75rem; font-weight: 700; color: var(--text); }
.miniChart { display: flex; align-items: flex-end; gap: 2px; height: 18px; }
.miniBar { width: 5px; border-radius: 2px 2px 0 0; background: rgba(139,92,246,0.3); }
.miniBarLast { background: var(--primary); }
.phase2Header { font-size: 0.62rem; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-dim); padding: 8px 2px 4px; opacity: 0.5; }
```

- [ ] **3.4 TabKognitiv.jsx: ModuleList einbinden**

```jsx
// Imports ergänzen:
import ModuleList from './ModuleList'

// In TabKognitiv, onSelectModule:
const handleSelectModule = (moduleId) => {
  if (isDoneToday(moduleId)) {
    setNav({ screen: 'done-today', moduleId })
  } else {
    setNav({ screen: 'briefing', moduleId })
  }
}

// Im JSX (Module-Tab):
{tab === 'modules' && <ModuleList onSelectModule={handleSelectModule} />}
```

isDoneToday importieren: `import { isDoneToday } from './sessionStore'`

- [ ] **3.5 Testen** — Kognitiv-Tab öffnen → Module-Tab zeigt 3 aktive + 3 gedimmte Karten. Klick → Placeholder.

- [ ] **3.6 Commit**

```bash
git add src/features/tools/kognitiv/
git commit -m "feat(kognitiv): ModuleList — Modul-Karten, Phase-2-Platzhalter"
```

---

## Task 4: Briefing + DoneToday

**Files:**
- Create: `src/features/tools/kognitiv/Briefing.jsx`
- Create: `src/features/tools/kognitiv/Briefing.module.css`
- Create: `src/features/tools/kognitiv/DoneToday.jsx`
- Create: `src/features/tools/kognitiv/DoneToday.module.css`
- Modify: `src/features/tools/kognitiv/TabKognitiv.jsx`

- [ ] **4.1 Briefing.jsx erstellen**

```jsx
// src/features/tools/kognitiv/Briefing.jsx
import { useState } from 'react'
import { MODULE_CONFIG } from './moduleConfig'
import { ToolIcon } from '../toolRegistry'
import s from './Briefing.module.css'

export default function Briefing({ moduleId, onStart, onBack }) {
  const m = MODULE_CONFIG[moduleId]
  const [variant, setVariant] = useState(m.defaultVariant)

  return (
    <div className={s.root}>
      <button className={s.backBtn} onClick={onBack}>‹</button>

      <div className={s.iconWrap}>
        <ToolIcon id="kognitiv" size={24} />
      </div>
      <div className={s.name}>{m.name}</div>
      <div className={s.durationPill}>
        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
        {m.duration}
      </div>
      <p className={s.desc}>{m.desc}</p>

      <div className={s.infoBlock}>
        <div className={s.infoLabel} data-type="measured">Was gemessen wird</div>
        {m.measured.map(item => (
          <div key={item} className={s.infoRow}>
            <div className={s.dot} data-type="measured" />
            {item}
          </div>
        ))}
      </div>

      <div className={s.infoBlock}>
        <div className={s.infoLabel} data-type="not">Nicht relevant</div>
        {m.notMeasured.map(item => (
          <div key={item} className={s.infoRow}>
            <div className={s.dot} data-type="not" />
            {item}
          </div>
        ))}
      </div>

      <div className={s.diffLabel}>Schwierigkeit</div>
      <div className={s.diffRow}>
        {m.variants.map(v => (
          <button
            key={v}
            className={[s.diffBtn, variant === v ? s.diffOn : ''].join(' ')}
            onClick={() => setVariant(v)}
          >
            {v}
          </button>
        ))}
      </div>

      <button className={s.startBtn} onClick={() => onStart(variant)}>
        Starten →
      </button>
    </div>
  )
}
```

- [ ] **4.2 Briefing.module.css erstellen**

```css
.root { padding: 20px 16px; display: flex; flex-direction: column; min-height: 100%; background: var(--bg); }
.backBtn { width: 28px; height: 28px; border-radius: var(--r-sm); background: var(--surface); border: 1px solid var(--border); color: var(--text-dim); font-size: 0.9rem; display: flex; align-items: center; justify-content: center; cursor: pointer; margin-bottom: 16px; }
.iconWrap { width: 52px; height: 52px; border-radius: 15px; background: rgba(139,92,246,0.1); border: 1px solid rgba(139,92,246,0.18); display: flex; align-items: center; justify-content: center; color: var(--primary); margin-bottom: 10px; }
.name { font-size: 1.2rem; font-weight: 700; color: var(--text); margin-bottom: 4px; }
.durationPill { display: inline-flex; align-items: center; gap: 5px; background: var(--surface); border: 1px solid var(--border); border-radius: 20px; padding: 3px 10px; font-size: 0.68rem; color: var(--text-dim); margin-bottom: 12px; }
.desc { font-size: 0.78rem; color: var(--text-dim); line-height: 1.6; margin-bottom: 14px; }
.infoBlock { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-sm); padding: 10px 12px; margin-bottom: 8px; }
.infoLabel { font-size: 0.6rem; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; margin-bottom: 6px; }
.infoLabel[data-type="measured"] { color: var(--emerald); }
.infoLabel[data-type="not"] { color: var(--text-dim); }
.infoRow { display: flex; align-items: flex-start; gap: 7px; font-size: 0.72rem; color: var(--text-dim); margin-bottom: 3px; line-height: 1.4; }
.dot { width: 5px; height: 5px; border-radius: 50%; flex-shrink: 0; margin-top: 5px; }
.dot[data-type="measured"] { background: var(--emerald); }
.dot[data-type="not"] { background: var(--border); }
.diffLabel { font-size: 0.62rem; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-dim); margin: 12px 0 6px; }
.diffRow { display: flex; gap: 6px; margin-bottom: 16px; }
.diffBtn { flex: 1; padding: 7px; border-radius: var(--r-sm); font-family: 'Outfit', sans-serif; font-size: 0.72rem; font-weight: 600; border: 1px solid var(--border); background: var(--surface); color: var(--text-dim); cursor: pointer; transition: all 0.15s; }
.diffOn { background: rgba(139,92,246,0.14); border-color: rgba(139,92,246,0.35); color: var(--primary); }
.startBtn { margin-top: auto; background: var(--primary); border: none; border-radius: var(--r); color: white; font-family: 'Outfit', sans-serif; font-size: 0.9rem; font-weight: 700; padding: 14px; text-align: center; width: 100%; cursor: pointer; }
```

- [ ] **4.3 DoneToday.jsx erstellen**

```jsx
// src/features/tools/kognitiv/DoneToday.jsx
import { MODULE_CONFIG } from './moduleConfig'
import { getLastSession } from './sessionStore'
import s from './DoneToday.module.css'

export default function DoneToday({ moduleId, onBack, onViewResult }) {
  const m = MODULE_CONFIG[moduleId]
  const last = getLastSession(moduleId)

  const time = last ? new Date(last.startedAt).toLocaleTimeString('de-DE', { hour: '2-digit', minute: '2-digit' }) : ''

  return (
    <div className={s.root}>
      <button className={s.backBtn} onClick={onBack}>‹</button>
      <div className={s.center}>
        <div className={s.checkIcon}>
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        <div className={s.msg}>Heute erledigt</div>
        <div className={s.sub}>Dein Gehirn braucht Zeit zum Verarbeiten. Morgen wieder.</div>
        <div className={s.nextPill}>Morgen ab 00:00 wieder verfügbar</div>
      </div>
      {last && (
        <div className={s.lastBox}>
          <div className={s.lastLabel}>Letzte Session — heute {time}</div>
          <div className={s.lastRow}>
            <div>
              <div className={s.lastVal}>{last.mainMetric}{m.mainMetricUnit}</div>
              <div className={s.lastSub}>{m.mainMetricLabel}</div>
            </div>
            <button className={s.viewBtn} onClick={() => onViewResult(last)}>Ergebnis ansehen →</button>
          </div>
        </div>
      )}
    </div>
  )
}
```

- [ ] **4.4 DoneToday.module.css erstellen**

```css
.root { padding: 20px 16px; display: flex; flex-direction: column; min-height: 100%; background: var(--bg); }
.backBtn { width: 28px; height: 28px; border-radius: var(--r-sm); background: var(--surface); border: 1px solid var(--border); color: var(--text-dim); font-size: 0.9rem; display: flex; align-items: center; justify-content: center; cursor: pointer; margin-bottom: auto; }
.center { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; gap: 12px; padding: 24px 0; }
.checkIcon { width: 64px; height: 64px; border-radius: 18px; background: rgba(16,185,129,0.1); border: 1px solid rgba(16,185,129,0.2); display: flex; align-items: center; justify-content: center; color: var(--emerald); }
.msg { font-size: 1.1rem; font-weight: 700; color: var(--text); }
.sub { font-size: 0.75rem; color: var(--text-dim); line-height: 1.6; max-width: 220px; }
.nextPill { font-family: 'Orbitron', sans-serif; font-size: 0.62rem; color: var(--emerald); background: rgba(16,185,129,0.08); border: 1px solid rgba(16,185,129,0.15); border-radius: 20px; padding: 5px 14px; font-weight: 600; }
.lastBox { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r); padding: 12px 14px; }
.lastLabel { font-size: 0.6rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 8px; }
.lastRow { display: flex; justify-content: space-between; align-items: center; }
.lastVal { font-family: 'Orbitron', sans-serif; font-size: 1.1rem; font-weight: 700; color: var(--text); }
.lastSub { font-size: 0.62rem; color: var(--text-dim); margin-top: 2px; }
.viewBtn { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-sm); color: var(--text-dim); font-family: 'Outfit', sans-serif; font-size: 0.72rem; font-weight: 600; padding: 7px 12px; cursor: pointer; }
```

- [ ] **4.5 TabKognitiv.jsx: Briefing + DoneToday einbinden**

```jsx
// Imports ergänzen:
import Briefing  from './Briefing'
import DoneToday from './DoneToday'

// nav-Screens ersetzen:
if (nav?.screen === 'briefing') {
  return <Briefing
    moduleId={nav.moduleId}
    onBack={goBack}
    onStart={(variant) => setNav({ screen: 'exercise', moduleId: nav.moduleId, variant })}
  />
}
if (nav?.screen === 'done-today') {
  return <DoneToday
    moduleId={nav.moduleId}
    onBack={goBack}
    onViewResult={(session) => setNav({ screen: 'results', session, fromArchive: true })}
  />
}
```

- [ ] **4.6 Testen** — Modul klicken → Briefing erscheint. Variante wechseln. Starten → Placeholder. Zweites Mal klicken wenn DoneToday aktiv → DoneToday-Screen.

- [ ] **4.7 Commit**

```bash
git add src/features/tools/kognitiv/
git commit -m "feat(kognitiv): Briefing + DoneToday screens"
```

---

## Task 5: AlertnessExercise

**Files:**
- Create: `src/features/tools/kognitiv/exercises/AlertnessExercise.jsx`
- Create: `src/features/tools/kognitiv/exercises/AlertnessExercise.module.css`

- [ ] **5.1 AlertnessExercise.jsx erstellen**

```jsx
// src/features/tools/kognitiv/exercises/AlertnessExercise.jsx
import { useEffect, useRef, useState, useCallback } from 'react'
import { createSession } from '../sessionStore'
import s from './AlertnessExercise.module.css'

const TOTAL_STIMULI = 30
const ISI_NORMAL = [2000, 5000]   // ms range
const ISI_SCHWER = [800, 2500]
const STIMULUS_VISIBLE = 800      // ms
const WARN_GAP = [500, 2000]      // ms between beep and stimulus

function randBetween(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min
}

export default function AlertnessExercise({ variant, onDone, onAbort }) {
  const mitTon        = variant === 'Mit Warnton'
  const isiRange      = variant === 'Schwer' ? ISI_SCHWER : ISI_NORMAL
  const [visible, setVisible] = useState(false)
  const tapsRef       = useRef([])
  const stimulusCount = useRef(0)
  const startedAt     = useRef(new Date().toISOString())
  const timerRef      = useRef(null)
  const audioCtx      = useRef(null)

  const playBeep = useCallback(() => {
    if (!mitTon) return
    try {
      if (!audioCtx.current) audioCtx.current = new AudioContext()
      const ctx = audioCtx.current
      const osc = ctx.createOscillator()
      const gain = ctx.createGain()
      osc.connect(gain)
      gain.connect(ctx.destination)
      osc.frequency.value = 880
      gain.gain.setValueAtTime(0.3, ctx.currentTime)
      gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.15)
      osc.start(ctx.currentTime)
      osc.stop(ctx.currentTime + 0.15)
    } catch {}
  }, [mitTon])

  const showStimulus = useCallback(() => {
    if (stimulusCount.current >= TOTAL_STIMULI) {
      finishSession()
      return
    }
    stimulusCount.current += 1
    const appearedAt = Date.now()
    setVisible(true)
    timerRef.current = setTimeout(() => {
      setVisible(false)
      tapsRef.current.push({ index: stimulusCount.current - 1, correct: false, time: null, type: 'miss' })
      scheduleNext()
    }, STIMULUS_VISIBLE)

    // store appearedAt for tap time calculation
    timerRef.current._appearedAt = appearedAt
  }, [])

  const scheduleNext = useCallback(() => {
    const isi = randBetween(...isiRange)
    if (mitTon) {
      const warnDelay = isi - randBetween(...WARN_GAP)
      setTimeout(playBeep, Math.max(0, warnDelay))
    }
    timerRef.current = setTimeout(showStimulus, isi)
  }, [isiRange, mitTon, playBeep, showStimulus])

  useEffect(() => {
    scheduleNext()
    return () => clearTimeout(timerRef.current)
  }, [])

  const finishSession = useCallback(() => {
    clearTimeout(timerRef.current)
    const taps = tapsRef.current.filter(t => t.type === 'hit')
    const avgMs = taps.length > 0 ? Math.round(taps.reduce((a, b) => a + b.time, 0) / taps.length) : 0
    const hits   = tapsRef.current.filter(t => t.type === 'hit').length
    const misses = tapsRef.current.filter(t => t.type === 'miss').length
    const falseAlarms = tapsRef.current.filter(t => t.type === 'false-alarm').length
    const duration = Math.round((Date.now() - new Date(startedAt.current).getTime()) / 1000)
    const session = createSession({
      moduleId: 'alertness',
      variant,
      startedAt: startedAt.current,
      duration,
      score: { hits, misses, falseAlarms, total: TOTAL_STIMULI },
      mainMetric: avgMs,
      taps: tapsRef.current,
    })
    onDone(session)
  }, [variant, onDone])

  const handleTap = useCallback(() => {
    if (visible) {
      // valid hit
      const rt = Date.now() - (timerRef.current?._appearedAt ?? Date.now())
      clearTimeout(timerRef.current)
      tapsRef.current.push({ index: stimulusCount.current - 1, correct: true, time: rt, type: 'hit' })
      setVisible(false)
      scheduleNext()
      if (stimulusCount.current >= TOTAL_STIMULI) finishSession()
    } else {
      // false alarm
      tapsRef.current.push({ index: stimulusCount.current, correct: false, time: null, type: 'false-alarm' })
    }
  }, [visible, scheduleNext, finishSession])

  return (
    <div className={s.root} onClick={handleTap}>
      <button className={s.closeBtn} onClick={e => { e.stopPropagation(); onAbort() }}>✕</button>
      <div className={s.arena}>
        {visible && <div className={s.stimulus} />}
      </div>
    </div>
  )
}
```

- [ ] **5.2 AlertnessExercise.module.css erstellen**

```css
.root { position: fixed; inset: 0; background: #05050e; display: flex; flex-direction: column; align-items: center; justify-content: center; cursor: pointer; user-select: none; }
.closeBtn { position: absolute; top: 16px; left: 16px; width: 28px; height: 28px; border-radius: 8px; background: rgba(255,255,255,0.04); border: none; color: rgba(255,255,255,0.18); font-size: 0.75rem; display: flex; align-items: center; justify-content: center; cursor: pointer; z-index: 10; }
.arena { flex: 1; width: 100%; display: flex; align-items: center; justify-content: center; }
.stimulus { width: 100px; height: 100px; border-radius: 50%; background: radial-gradient(circle at 38% 38%, rgba(139,92,246,0.95), #8B5CF6); box-shadow: 0 0 40px rgba(139,92,246,0.5), 0 0 80px rgba(139,92,246,0.2); animation: appear 0.1s ease-out; }
@keyframes appear { from { transform: scale(0.7); opacity: 0; } to { transform: scale(1); opacity: 1; } }
```

- [ ] **5.3 TabKognitiv.jsx: AlertnessExercise einbinden**

```jsx
import AlertnessExercise from './exercises/AlertnessExercise'

// Exercise-Screen ersetzen:
if (nav?.screen === 'exercise') {
  const ExMap = { alertness: AlertnessExercise }
  const Ex = ExMap[nav.moduleId]
  if (Ex) return <Ex
    variant={nav.variant}
    onDone={(session) => setNav({ screen: 'results', session })}
    onAbort={goBack}
  />
  return <div className={s.placeholder}>Exercise {nav.moduleId} — noch nicht implementiert</div>
}
```

- [ ] **5.4 Testen** — Alertness → Briefing → Starten → schwarzer Screen, Kreis erscheint, tippen → nach 30 Stimuli → Results-Placeholder.

- [ ] **5.5 Commit**

```bash
git add src/features/tools/kognitiv/exercises/AlertnessExercise.jsx src/features/tools/kognitiv/exercises/AlertnessExercise.module.css src/features/tools/kognitiv/TabKognitiv.jsx
git commit -m "feat(kognitiv): AlertnessExercise — stimulus tap, beep variant, session data"
```

---

## Task 6: ZahlensucheExercise

**Files:**
- Create: `src/features/tools/kognitiv/exercises/ZahlensucheExercise.jsx`
- Create: `src/features/tools/kognitiv/exercises/ZahlensucheExercise.module.css`

- [ ] **6.1 ZahlensucheExercise.jsx erstellen**

```jsx
// src/features/tools/kognitiv/exercises/ZahlensucheExercise.jsx
import { useState, useRef, useEffect, useCallback } from 'react'
import { createSession } from '../sessionStore'
import s from './ZahlensucheExercise.module.css'

function shuffle(arr) {
  const a = [...arr]
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]]
  }
  return a
}

export default function ZahlensucheExercise({ variant, onDone, onAbort }) {
  const isReverse = variant === 'Rückwärts'
  const isHard    = variant === 'Schwer'
  const total     = isHard ? 30 : 25
  const cols      = isHard ? 6 : 5
  const rows      = isHard ? 5 : 5

  const numbers = Array.from({ length: total }, (_, i) => i + 1)
  const sequence = isReverse ? [...numbers].reverse() : numbers   // target order
  const [cells]  = useState(() => shuffle(numbers))

  const [nextIdx, setNextIdx] = useState(0)     // index in sequence
  const [doneSet, setDoneSet] = useState(new Set())
  const [errorCell, setErrorCell] = useState(null)
  const tapsRef  = useRef([])
  const startRef = useRef(Date.now())
  const startedAt = useRef(new Date().toISOString())
  const lastTapRef = useRef(Date.now())

  const handleTap = useCallback((num) => {
    const now = Date.now()
    const elapsed = (now - lastTapRef.current) / 1000
    lastTapRef.current = now
    const target = sequence[nextIdx]
    const correct = num === target

    tapsRef.current.push({ index: nextIdx, target, got: num, correct, time: elapsed })

    if (correct) {
      setDoneSet(prev => new Set([...prev, num]))
      const newIdx = nextIdx + 1
      setNextIdx(newIdx)
      if (newIdx >= total) {
        // finished
        const totalTime = Math.round((now - startRef.current) / 1000)
        const errors = tapsRef.current.filter(t => !t.correct).length
        const session = createSession({
          moduleId: 'zahlensuche',
          variant,
          startedAt: startedAt.current,
          duration: totalTime,
          score: { correct: total, errors, total },
          mainMetric: totalTime,
          taps: tapsRef.current,
        })
        onDone(session)
      }
    } else {
      setErrorCell(num)
      setTimeout(() => setErrorCell(null), 500)
    }
  }, [nextIdx, sequence, total, variant, onDone])

  const fmt = (n) => String(n).padStart(2, '0')
  const nextTarget = sequence[nextIdx]

  return (
    <div className={s.root}>
      <div className={s.topBar}>
        <button className={s.closeBtn} onClick={onAbort}>✕</button>
      </div>
      <div className={s.grid} style={{ gridTemplateColumns: `repeat(${cols}, 1fr)` }}>
        {cells.map(num => {
          const isDone  = doneSet.has(num)
          const isNext  = num === nextTarget
          const isError = num === errorCell
          return (
            <button
              key={num}
              className={[s.cell, isDone ? s.done : '', isNext ? s.next : '', isError ? s.error : ''].join(' ')}
              onClick={() => !isDone && handleTap(num)}
              disabled={isDone}
            >
              {fmt(num)}
            </button>
          )
        })}
      </div>
    </div>
  )
}
```

- [ ] **6.2 ZahlensucheExercise.module.css erstellen**

```css
.root { position: fixed; inset: 0; background: #05050e; display: flex; flex-direction: column; }
.topBar { padding: 14px 14px 0; display: flex; }
.closeBtn { width: 28px; height: 28px; border-radius: 8px; background: rgba(255,255,255,0.04); border: none; color: rgba(255,255,255,0.18); font-size: 0.75rem; cursor: pointer; }
.grid { flex: 1; display: grid; gap: 5px; padding: 12px; align-content: center; }
.cell { aspect-ratio: 1; border-radius: 9px; background: rgba(255,255,255,0.04); border: 1px solid rgba(255,255,255,0.06); color: rgba(255,255,255,0.65); font-family: 'Orbitron', sans-serif; font-size: 0.58rem; font-weight: 700; cursor: pointer; display: flex; align-items: center; justify-content: center; transition: all 0.1s; }
.done { background: rgba(139,92,246,0.1); border-color: rgba(139,92,246,0.18); color: rgba(139,92,246,0.4); cursor: default; }
.next { background: rgba(139,92,246,0.22); border-color: rgba(139,92,246,0.7); color: rgba(255,255,255,0.95); box-shadow: 0 0 14px rgba(139,92,246,0.4); animation: pulse 1.1s ease-in-out infinite; }
.error { background: rgba(251,113,133,0.2); border-color: rgba(251,113,133,0.5); color: #FB7185; }
@keyframes pulse { 0%,100% { box-shadow: 0 0 10px rgba(139,92,246,0.35); } 50% { box-shadow: 0 0 22px rgba(139,92,246,0.65); } }
```

- [ ] **6.3 TabKognitiv.jsx: ZahlensucheExercise einbinden**

```jsx
import ZahlensucheExercise from './exercises/ZahlensucheExercise'

// ExMap ergänzen:
const ExMap = {
  alertness:   AlertnessExercise,
  zahlensuche: ZahlensucheExercise,
}
```

- [ ] **6.4 Testen** — Zahlensuche → Normal: 5×5 Grid, 01–25 antippen in Reihenfolge. Falsche Zahl: kurzer roter Blink. Schwer: 6×5 Grid mit 01–30. Rückwärts: 25→01.

- [ ] **6.5 Commit**

```bash
git add src/features/tools/kognitiv/exercises/ZahlensucheExercise.jsx src/features/tools/kognitiv/exercises/ZahlensucheExercise.module.css src/features/tools/kognitiv/TabKognitiv.jsx
git commit -m "feat(kognitiv): ZahlensucheExercise — grid, shuffle, tap timing, variants"
```

---

## Task 7: GedaechtnisExercise

**Files:**
- Create: `src/features/tools/kognitiv/exercises/GedaechtnisExercise.jsx`
- Create: `src/features/tools/kognitiv/exercises/GedaechtnisExercise.module.css`

- [ ] **7.1 GedaechtnisExercise.jsx erstellen**

```jsx
// src/features/tools/kognitiv/exercises/GedaechtnisExercise.jsx
import { useState, useRef, useEffect, useCallback } from 'react'
import { createSession } from '../sessionStore'
import s from './GedaechtnisExercise.module.css'

// 12 circles arranged as clock face (positions 0–11 = 12,1,2...11 o'clock)
const CIRCLE_POSITIONS = Array.from({ length: 12 }, (_, i) => {
  const angle = (i * 30 - 90) * (Math.PI / 180)
  const r = 38  // % radius
  return { left: `${50 + r * Math.cos(angle)}%`, top: `${50 + r * Math.sin(angle)}%` }
})

// Schema: 2×2, 2×3, 2×4, 2×5 = 8 rounds
const ROUNDS = [2, 2, 3, 3, 4, 4, 5, 5]
const SHOW_MS = 600   // each circle lit duration
const GAP_MS  = 200   // gap between circles in sequence

function randSeq(len) {
  const pool = Array.from({ length: 12 }, (_, i) => i)
  const seq = []
  while (seq.length < len) {
    const idx = Math.floor(Math.random() * pool.length)
    if (!seq.includes(pool[idx])) seq.push(pool[idx])
  }
  return seq
}

export default function GedaechtnisExercise({ variant, onDone, onAbort }) {
  const isHard = variant === 'Schwer'
  const showMs = isHard ? 400 : SHOW_MS
  const gapMs  = isHard ? 100 : GAP_MS

  const [phase, setPhase] = useState('show')       // 'show' | 'input'
  const [roundIdx, setRoundIdx] = useState(0)
  const [litCircle, setLitCircle] = useState(null)
  const [userInput, setUserInput] = useState([])
  const [highlight, setHighlight] = useState(null) // { idx, correct }
  const seqRef     = useRef(null)
  const tapsRef    = useRef([])
  const startedAt  = useRef(new Date().toISOString())

  const currentSeqLen = ROUNDS[roundIdx]

  // Play sequence
  const playSequence = useCallback((seq) => {
    setPhase('show')
    setUserInput([])
    seq.forEach((circIdx, i) => {
      setTimeout(() => setLitCircle(circIdx), i * (showMs + gapMs))
      setTimeout(() => setLitCircle(null), i * (showMs + gapMs) + showMs)
    })
    setTimeout(() => setPhase('input'), seq.length * (showMs + gapMs) + 200)
  }, [showMs, gapMs])

  useEffect(() => {
    const seq = randSeq(currentSeqLen)
    seqRef.current = seq
    playSequence(seq)
  }, [roundIdx])

  const handleCircleTap = useCallback((circIdx) => {
    if (phase !== 'input') return
    const pos = userInput.length
    const expected = seqRef.current[pos]
    const correct = circIdx === expected
    setHighlight({ idx: circIdx, correct })
    setTimeout(() => setHighlight(null), 300)

    const newInput = [...userInput, circIdx]
    setUserInput(newInput)

    tapsRef.current.push({ round: roundIdx, pos, expected, got: circIdx, correct })

    if (newInput.length === currentSeqLen) {
      // round complete
      const nextRound = roundIdx + 1
      if (nextRound >= ROUNDS.length) {
        // all done
        const correct = tapsRef.current.filter(t => t.correct).length
        const duration = Math.round((Date.now() - new Date(startedAt.current).getTime()) / 1000)
        const roundResults = ROUNDS.map((len, ri) => {
          const rTaps = tapsRef.current.filter(t => t.round === ri)
          return { round: ri, seqLen: len, errors: rTaps.filter(t => !t.correct).length }
        })
        const correctRounds = roundResults.filter(r => r.errors === 0).length
        const session = createSession({
          moduleId: 'gedaechtnis',
          variant,
          startedAt: startedAt.current,
          duration,
          score: { correctRounds, totalRounds: ROUNDS.length, correctTaps: correct },
          mainMetric: correctRounds,
          taps: tapsRef.current,
        })
        onDone(session)
      } else {
        setTimeout(() => setRoundIdx(nextRound), 600)
      }
    }
  }, [phase, userInput, currentSeqLen, roundIdx, variant, onDone])

  return (
    <div className={s.root}>
      <button className={s.closeBtn} onClick={onAbort}>✕</button>
      <div className={s.roundInfo}>
        {phase === 'show' ? `Einprägen — ${currentSeqLen} Kreise` : `Reproduzieren — ${currentSeqLen} Kreise`}
      </div>
      <div className={s.arena}>
        {CIRCLE_POSITIONS.map((pos, i) => {
          const isLit = litCircle === i
          const hlState = highlight?.idx === i ? (highlight.correct ? 'correct' : 'wrong') : null
          return (
            <button
              key={i}
              className={[s.circle, isLit ? s.lit : '', hlState ? s[hlState] : ''].join(' ')}
              style={pos}
              onClick={() => handleCircleTap(i)}
            />
          )
        })}
      </div>
      <div className={s.progress}>Runde {roundIdx + 1} / {ROUNDS.length}</div>
    </div>
  )
}
```

- [ ] **7.2 GedaechtnisExercise.module.css erstellen**

```css
.root { position: fixed; inset: 0; background: #05050e; display: flex; flex-direction: column; align-items: center; }
.closeBtn { position: absolute; top: 16px; left: 16px; width: 28px; height: 28px; border-radius: 8px; background: rgba(255,255,255,0.04); border: none; color: rgba(255,255,255,0.18); font-size: 0.75rem; cursor: pointer; }
.roundInfo { margin-top: 60px; font-size: 0.72rem; color: rgba(255,255,255,0.3); letter-spacing: 0.08em; text-transform: uppercase; }
.arena { flex: 1; position: relative; width: 100%; max-width: 340px; }
.circle { position: absolute; transform: translate(-50%, -50%); width: 48px; height: 48px; border-radius: 50%; background: rgba(255,255,255,0.06); border: 1.5px solid rgba(255,255,255,0.1); cursor: pointer; transition: all 0.1s; }
.lit { background: var(--primary); border-color: var(--primary); box-shadow: 0 0 20px rgba(139,92,246,0.6); }
.correct { background: var(--emerald); border-color: var(--emerald); }
.wrong { background: var(--rose); border-color: var(--rose); }
.progress { padding-bottom: 32px; font-family: 'Orbitron', sans-serif; font-size: 0.62rem; color: rgba(255,255,255,0.2); font-weight: 600; }
```

- [ ] **7.3 TabKognitiv.jsx: GedaechtnisExercise einbinden**

```jsx
import GedaechtnisExercise from './exercises/GedaechtnisExercise'

const ExMap = {
  alertness:   AlertnessExercise,
  zahlensuche: ZahlensucheExercise,
  gedaechtnis: GedaechtnisExercise,
}
```

- [ ] **7.4 Testen** — Gedächtnis → Briefing → Starten. Sequenz leuchtet auf → reproduzieren → nächste Runde. Nach 8 Runden → Results-Placeholder.

- [ ] **7.5 Commit**

```bash
git add src/features/tools/kognitiv/exercises/
git commit -m "feat(kognitiv): GedaechtnisExercise — 12-circle ring, sequence show/input, 8 rounds"
```

---

## Task 8: Results

**Files:**
- Create: `src/features/tools/kognitiv/Results.jsx`
- Create: `src/features/tools/kognitiv/Results.module.css`
- Modify: `src/features/tools/kognitiv/TabKognitiv.jsx`

- [ ] **8.1 Results.jsx erstellen**

```jsx
// src/features/tools/kognitiv/Results.jsx
import { useAppStore } from '../../../store'
import { MODULE_CONFIG } from './moduleConfig'
import { getDelta, saveSession, getModuleSessions } from './sessionStore'
import s from './Results.module.css'

export default function Results({ session, fromArchive = false, onBack, onSaveToCalendar }) {
  const m = MODULE_CONFIG[session.moduleId]
  const delta = getDelta(session.moduleId, session.mainMetric)
  const allSessions = getModuleSessions(session.moduleId)
  const last7 = allSessions.slice(-7)

  // Tap timeline for Zahlensuche / Alertness
  const taps = session.taps ?? []
  const hitTimes = taps.filter(t => t.correct && t.time != null).map(t => t.time)
  const maxTime  = Math.max(...hitTimes, 1)

  const deltaLabel = delta !== null
    ? (delta > 0 ? `▲ ${Math.abs(delta)}${m.mainMetricUnit} besser` : `▼ ${Math.abs(delta)}${m.mainMetricUnit} schlechter`)
    : null

  return (
    <div className={s.root}>
      <button className={s.backBtn} onClick={onBack}>‹</button>
      <div className={s.eyebrow}>{fromArchive ? 'Archiv' : 'Fertig'}</div>
      <div className={s.title}>{m.name}</div>

      <div className={s.hero}>
        <div className={s.heroLeft}>
          <div className={s.score}>{session.mainMetric}</div>
          <div className={s.scoreLabel}>{m.mainMetricLabel} · {m.mainMetricUnit}</div>
          {deltaLabel && (
            <div className={[s.delta, delta > 0 ? s.deltaGood : s.deltaBad].join(' ')}>
              {deltaLabel}
            </div>
          )}
        </div>
        {last7.length > 1 && (
          <div className={s.heroRight}>
            <div className={s.trendLabel}>Verlauf</div>
            <div className={s.trendBars}>
              {last7.map((sess, i) => (
                <div
                  key={i}
                  className={[s.trendBar, i === last7.length - 1 ? s.trendBarLast : ''].join(' ')}
                  style={{ height: `${Math.max(15, (Math.min(...last7.map(x=>x.mainMetric)) / sess.mainMetric) * 100)}%` }}
                />
              ))}
            </div>
          </div>
        )}
      </div>

      <div className={s.metrics}>
        <div className={s.metric}>
          <div className={s.mVal} style={{ color: 'var(--emerald)' }}>
            {session.score.correct ?? session.score.correctRounds ?? '—'}
          </div>
          <div className={s.mLbl}>Korrekte</div>
        </div>
        <div className={s.metric}>
          <div className={s.mVal} style={{ color: 'var(--rose)' }}>
            {session.score.errors ?? '—'}
          </div>
          <div className={s.mLbl}>Fehler</div>
        </div>
        <div className={s.metric}>
          <div className={s.mVal}>{session.duration}s</div>
          <div className={s.mLbl}>Dauer</div>
        </div>
      </div>

      {hitTimes.length > 0 && (
        <div className={s.tapSection}>
          <div className={s.tapLabel}>Tipp-Zeiten</div>
          <div className={s.tapBars}>
            {taps.map((tap, i) => {
              const t = tap.time ?? 0
              const h = Math.max(10, Math.min(100, (t / maxTime) * 100))
              const color = !tap.correct ? 'var(--rose)'
                : t < maxTime * 0.4 ? 'var(--emerald)'
                : 'rgba(139,92,246,0.5)'
              return <div key={i} className={s.tapBar} style={{ height: `${h}%`, background: color }} />
            })}
          </div>
        </div>
      )}

      {!fromArchive && (
        <button className={s.calBtn} onClick={() => onSaveToCalendar(session)}>
          Im Kalender speichern
        </button>
      )}
      <button className={s.backBtn2} onClick={onBack}>
        {fromArchive ? 'Zurück' : 'Fertig'}
      </button>
    </div>
  )
}
```

- [ ] **8.2 Results.module.css erstellen**

```css
.root { padding: 16px 14px 24px; display: flex; flex-direction: column; min-height: 100%; background: var(--bg); overflow-y: auto; }
.backBtn { width: 28px; height: 28px; border-radius: var(--r-sm); background: var(--surface); border: 1px solid var(--border); color: var(--text-dim); font-size: 0.9rem; display: flex; align-items: center; justify-content: center; cursor: pointer; margin-bottom: 10px; }
.eyebrow { font-size: 0.6rem; font-weight: 600; letter-spacing: 0.12em; text-transform: uppercase; color: var(--emerald); margin-bottom: 2px; }
.title { font-size: 1.1rem; font-weight: 700; color: var(--text); margin-bottom: 12px; }
.hero { background: rgba(139,92,246,0.07); border: 1px solid rgba(139,92,246,0.14); border-radius: var(--r); padding: 14px; display: flex; gap: 12px; margin-bottom: 10px; }
.heroLeft { flex: 1; }
.score { font-family: 'Orbitron', sans-serif; font-size: 2.2rem; font-weight: 700; color: var(--primary); line-height: 1; }
.scoreLabel { font-size: 0.62rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.08em; margin-top: 3px; }
.delta { display: inline-flex; align-items: center; border-radius: 16px; padding: 2px 8px; font-size: 0.68rem; font-weight: 600; margin-top: 7px; }
.deltaGood { background: rgba(16,185,129,0.1); border: 1px solid rgba(16,185,129,0.2); color: var(--emerald); }
.deltaBad  { background: rgba(251,113,133,0.1); border: 1px solid rgba(251,113,133,0.2); color: var(--rose); }
.heroRight { display: flex; flex-direction: column; align-items: flex-end; gap: 4px; }
.trendLabel { font-size: 0.58rem; color: var(--text-dim); }
.trendBars { display: flex; align-items: flex-end; gap: 3px; height: 36px; }
.trendBar { width: 8px; border-radius: 2px 2px 0 0; background: rgba(139,92,246,0.25); }
.trendBarLast { background: var(--primary); box-shadow: 0 0 6px rgba(139,92,246,0.4); }
.metrics { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 6px; margin-bottom: 10px; }
.metric { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-sm); padding: 8px; text-align: center; }
.mVal { font-family: 'Orbitron', sans-serif; font-size: 0.95rem; font-weight: 700; }
.mLbl { font-size: 0.58rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.06em; margin-top: 2px; }
.tapSection { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-sm); padding: 10px 12px; margin-bottom: 10px; }
.tapLabel { font-size: 0.6rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 8px; }
.tapBars { display: flex; align-items: flex-end; gap: 2.5px; height: 40px; }
.tapBar { flex: 1; border-radius: 2px 2px 0 0; min-width: 3px; }
.calBtn { background: var(--primary); border: none; border-radius: var(--r); color: white; font-family: 'Outfit', sans-serif; font-size: 0.88rem; font-weight: 700; padding: 13px; text-align: center; width: 100%; cursor: pointer; margin-bottom: 8px; }
.backBtn2 { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r); color: var(--text-dim); font-family: 'Outfit', sans-serif; font-size: 0.82rem; font-weight: 600; padding: 11px; text-align: center; width: 100%; cursor: pointer; }
```

- [ ] **8.3 TabKognitiv.jsx: Results einbinden + saveSession + Kalender**

```jsx
import { useState, useCallback } from 'react'
import { useAppStore } from '../../../store'
import Results from './Results'
import { saveSession } from './sessionStore'

// In TabKognitiv, add:
const { setDays } = useAppStore()

const handleSaveToCalendar = useCallback((session) => {
  const m = MODULE_CONFIG[session.moduleId]
  const hour = new Date(session.startedAt).getHours()
  const slotKey = String(hour)
  setDays(prev => ({
    ...prev,
    [session.date]: {
      ...(prev[session.date] ?? {}),
      [slotKey]: {
        text: `Kognitiv: ${m.name} (${session.mainMetric}${m.mainMetricUnit})`,
        color: '#8B5CF6',
        duration: Math.max(1, Math.ceil(session.duration / 60)),
        locked: true,
        done: true,
        toolId: 'kognitiv',
      }
    }
  }))
}, [setDays])

// Results nav-Screen ersetzen:
if (nav?.screen === 'results') {
  // save session on first view (not from archive)
  if (!nav.fromArchive && !nav.saved) {
    saveSession(nav.session)
    setNav(prev => ({ ...prev, saved: true }))
  }
  return <Results
    session={nav.session}
    fromArchive={nav.fromArchive}
    onBack={goBack}
    onSaveToCalendar={handleSaveToCalendar}
  />
}
```

Wichtig: `saveSession` darf nur einmal aufgerufen werden. Das `nav.saved`-Flag verhindert Doppelspeicherung bei Re-Render.

- [ ] **8.4 Testen** — Alle 3 Module durchspielen → Results-Screen erscheint mit Score, Delta (nach 2+ Sessions), Tap-Chart, Kalender-Button.

- [ ] **8.5 Commit**

```bash
git add src/features/tools/kognitiv/Results.jsx src/features/tools/kognitiv/Results.module.css src/features/tools/kognitiv/TabKognitiv.jsx
git commit -m "feat(kognitiv): Results screen — score, delta, tap timeline, save to calendar"
```

---

## Task 9: Dashboard + ModuleDetail + SessionDetail

**Files:**
- Create: `src/features/tools/kognitiv/Dashboard.jsx`
- Create: `src/features/tools/kognitiv/Dashboard.module.css`
- Create: `src/features/tools/kognitiv/ModuleDetail.jsx`
- Create: `src/features/tools/kognitiv/ModuleDetail.module.css`
- Create: `src/features/tools/kognitiv/SessionDetail.jsx`
- Create: `src/features/tools/kognitiv/SessionDetail.module.css`
- Modify: `src/features/tools/kognitiv/TabKognitiv.jsx`

- [ ] **9.1 Dashboard.jsx erstellen**

```jsx
// src/features/tools/kognitiv/Dashboard.jsx
import { loadSessions, getModuleStats, getWeeklyCount } from './sessionStore'
import { MODULE_CONFIG, MODULE_ORDER } from './moduleConfig'
import { ToolIcon } from '../toolRegistry'
import s from './Dashboard.module.css'

export default function Dashboard({ onSelectModule }) {
  const allSessions = loadSessions()
  const weeklyCount = getWeeklyCount()
  const trend = (() => {
    if (allSessions.length < 4) return '→'
    const half = Math.floor(allSessions.length / 2)
    const firstHalf = allSessions.slice(0, half)
    const secondHalf = allSessions.slice(half)
    // simple: compare average mainMetric of first vs second half
    // lower is better for time-based, higher for score-based
    // just show → for simplicity, can be refined later
    return '↑'
  })()

  return (
    <div className={s.root}>
      <div className={s.summary}>
        <div className={s.sumCol}>
          <div className={s.sumVal}>{allSessions.length}</div>
          <div className={s.sumLbl}>Sessions</div>
        </div>
        <div className={s.sumDivider} />
        <div className={s.sumCol}>
          <div className={s.sumVal}>{weeklyCount}</div>
          <div className={s.sumLbl}>Diese Woche</div>
        </div>
        <div className={s.sumDivider} />
        <div className={s.sumCol}>
          <div className={s.sumVal} style={{ color: 'var(--emerald)' }}>{trend}</div>
          <div className={s.sumLbl}>Trend</div>
        </div>
      </div>

      <div className={s.secLabel}>Module</div>

      {MODULE_ORDER.map(id => {
        const m = MODULE_CONFIG[id]
        const stats = getModuleStats(id)
        if (!stats) return (
          <div key={id} className={s.tile}>
            <div className={s.tileIcon}><ToolIcon id="kognitiv" size={15} /></div>
            <div className={s.tileInfo}>
              <div className={s.tileName}>{m.name}</div>
              <div className={s.tileLast}>Noch keine Session</div>
            </div>
          </div>
        )
        const last7 = stats.last7
        return (
          <button key={id} className={s.tile} onClick={() => onSelectModule(id)}>
            <div className={s.tileIcon}><ToolIcon id="kognitiv" size={15} /></div>
            <div className={s.tileInfo}>
              <div className={s.tileName}>{m.name}</div>
              <div className={s.tileLast}>Zuletzt: {stats.latest}{m.mainMetricUnit} · {stats.sessions} Sessions</div>
            </div>
            <div className={s.tileRight}>
              <div className={[s.tileDelta, stats.improvement > 0 ? s.deltaGood : s.deltaBad].join(' ')}>
                {stats.improvement > 0 ? '↑' : '↓'} {Math.abs(stats.improvement)}{m.mainMetricUnit}
              </div>
              <div className={s.miniChart}>
                {last7.map((sess, i) => (
                  <div
                    key={i}
                    className={[s.miniBar, i === last7.length - 1 ? s.miniBarLast : ''].join(' ')}
                    style={{ height: `${Math.max(20, (stats.best / sess.mainMetric) * 100)}%` }}
                  />
                ))}
              </div>
            </div>
          </button>
        )
      })}
    </div>
  )
}
```

- [ ] **9.2 Dashboard.module.css erstellen**

```css
.root { padding: 12px; display: flex; flex-direction: column; gap: 8px; }
.summary { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r); padding: 12px; display: flex; align-items: center; }
.sumCol { flex: 1; text-align: center; }
.sumVal { font-family: 'Orbitron', sans-serif; font-size: 1.2rem; font-weight: 700; color: var(--text); }
.sumLbl { font-size: 0.58rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.07em; margin-top: 2px; }
.sumDivider { width: 1px; height: 32px; background: var(--border); }
.secLabel { font-size: 0.62rem; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-dim); padding: 4px 2px; }
.tile { width: 100%; background: var(--surface); border: 1px solid var(--border); border-radius: var(--r); padding: 10px 12px; display: flex; align-items: center; gap: 10px; cursor: pointer; text-align: left; }
.tileIcon { width: 32px; height: 32px; border-radius: var(--r-sm); background: rgba(139,92,246,0.12); display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: var(--primary); }
.tileInfo { flex: 1; min-width: 0; }
.tileName { font-size: 0.8rem; font-weight: 600; color: var(--text); }
.tileLast { font-size: 0.62rem; color: var(--text-dim); margin-top: 1px; }
.tileRight { display: flex; flex-direction: column; align-items: flex-end; gap: 4px; flex-shrink: 0; }
.tileDelta { font-size: 0.65rem; font-weight: 600; border-radius: 10px; padding: 2px 7px; }
.deltaGood { background: rgba(16,185,129,0.1); color: var(--emerald); }
.deltaBad  { background: rgba(251,113,133,0.1); color: var(--rose); }
.miniChart { display: flex; align-items: flex-end; gap: 2px; height: 16px; }
.miniBar { width: 5px; border-radius: 2px 2px 0 0; background: rgba(139,92,246,0.25); }
.miniBarLast { background: var(--primary); }
```

- [ ] **9.3 ModuleDetail.jsx erstellen**

```jsx
// src/features/tools/kognitiv/ModuleDetail.jsx
import { MODULE_CONFIG } from './moduleConfig'
import { getModuleSessions, getModuleStats } from './sessionStore'
import s from './ModuleDetail.module.css'

export default function ModuleDetail({ moduleId, onBack, onSelectSession }) {
  const m = MODULE_CONFIG[moduleId]
  const sessions = getModuleSessions(moduleId).slice().reverse()  // newest first
  const stats    = getModuleStats(moduleId)
  const chronological = [...sessions].reverse()  // for chart
  const maxMetric = Math.max(...chronological.map(s => s.mainMetric), 1)
  const minMetric = Math.min(...chronological.map(s => s.mainMetric), 0)

  const fmtDate = (iso) => {
    const d = new Date(iso)
    return d.toLocaleDateString('de-DE', { day: '2-digit', month: '2-digit' })
  }
  const fmtTime = (iso) => new Date(iso).toLocaleTimeString('de-DE', { hour: '2-digit', minute: '2-digit' })

  return (
    <div className={s.root}>
      <div className={s.hd}>
        <button className={s.back} onClick={onBack}>‹</button>
        <div>
          <div className={s.ey}>Dashboard → Modul</div>
          <div className={s.tt}>{m.name}</div>
        </div>
      </div>

      {stats && (
        <>
          <div className={s.chart}>
            <div className={s.chartLabel}>{m.mainMetricLabel} ({m.mainMetricUnit}) — Verlauf</div>
            <div className={s.bars}>
              {chronological.map((sess, i) => {
                const h = Math.max(10, ((sess.mainMetric - minMetric) / (maxMetric - minMetric + 1)) * 100)
                const isLast = i === chronological.length - 1
                return <div key={sess.id} className={[s.bar, isLast ? s.barLast : ''].join(' ')} style={{ height: `${h}%` }} />
              })}
            </div>
          </div>

          <div className={s.statsRow}>
            <div className={s.stat}>
              <div className={s.statVal}>{stats.best}{m.mainMetricUnit}</div>
              <div className={s.statLbl}>Best</div>
            </div>
            <div className={s.stat}>
              <div className={s.statVal}>{stats.sessions}</div>
              <div className={s.statLbl}>Sessions</div>
            </div>
            <div className={s.stat}>
              <div className={s.statVal} style={{ color: 'var(--emerald)' }}>
                {stats.improvement > 0 ? `−${stats.improvement}` : `+${Math.abs(stats.improvement)}`}{m.mainMetricUnit}
              </div>
              <div className={s.statLbl}>Gesamt</div>
            </div>
          </div>
        </>
      )}

      <div className={s.listLabel}>Alle Sessions</div>
      <div className={s.list}>
        {sessions.map(sess => {
          const prev = sessions[sessions.indexOf(sess) + 1]
          const delta = prev ? prev.mainMetric - sess.mainMetric : null
          return (
            <button key={sess.id} className={s.sessRow} onClick={() => onSelectSession(sess)}>
              <div className={s.sessDate}>{fmtDate(sess.startedAt)} · {fmtTime(sess.startedAt)} · {sess.variant}</div>
              <div className={s.sessRight}>
                <span className={s.sessVal}>{sess.mainMetric}{m.mainMetricUnit}</span>
                {delta !== null && (
                  <span className={[s.sessDelta, delta > 0 ? s.deltaGood : s.deltaBad].join(' ')}>
                    {delta > 0 ? `▲${delta}` : `▼${Math.abs(delta)}`}
                  </span>
                )}
                <span className={s.sessArr}>›</span>
              </div>
            </button>
          )
        })}
      </div>
    </div>
  )
}
```

- [ ] **9.4 ModuleDetail.module.css erstellen**

```css
.root { padding: 0 12px 24px; display: flex; flex-direction: column; gap: 8px; background: var(--bg); min-height: 100%; }
.hd { padding: 16px 2px 8px; display: flex; align-items: center; gap: 10px; }
.back { width: 26px; height: 26px; border-radius: var(--r-sm); background: var(--surface); border: 1px solid var(--border); color: var(--text-dim); font-size: 0.9rem; cursor: pointer; display: flex; align-items: center; justify-content: center; }
.ey { font-size: 0.58rem; font-weight: 600; letter-spacing: 0.12em; text-transform: uppercase; color: var(--primary); }
.tt { font-size: 1rem; font-weight: 700; color: var(--text); }
.chart { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r); padding: 12px; }
.chartLabel { font-size: 0.6rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 10px; }
.bars { display: flex; align-items: flex-end; gap: 4px; height: 52px; }
.bar { flex: 1; border-radius: 3px 3px 0 0; background: rgba(139,92,246,0.25); min-width: 6px; }
.barLast { background: var(--primary); box-shadow: 0 0 8px rgba(139,92,246,0.4); }
.statsRow { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 6px; }
.stat { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-sm); padding: 8px; text-align: center; }
.statVal { font-family: 'Orbitron', sans-serif; font-size: 0.9rem; font-weight: 700; color: var(--text); }
.statLbl { font-size: 0.58rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.06em; margin-top: 2px; }
.listLabel { font-size: 0.62rem; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-dim); padding: 4px 2px; }
.list { display: flex; flex-direction: column; gap: 5px; }
.sessRow { width: 100%; background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-sm); padding: 9px 12px; display: flex; justify-content: space-between; align-items: center; cursor: pointer; text-align: left; }
.sessDate { font-size: 0.65rem; color: var(--text-dim); }
.sessRight { display: flex; align-items: center; gap: 6px; }
.sessVal { font-family: 'Orbitron', sans-serif; font-size: 0.75rem; font-weight: 700; color: var(--text); }
.sessDelta { font-size: 0.6rem; font-weight: 600; }
.deltaGood { color: var(--emerald); }
.deltaBad  { color: var(--rose); }
.sessArr { color: var(--text-dim); font-size: 0.8rem; }
```

- [ ] **9.5 SessionDetail.jsx erstellen**

```jsx
// src/features/tools/kognitiv/SessionDetail.jsx
import { MODULE_CONFIG } from './moduleConfig'
import s from './SessionDetail.module.css'

export default function SessionDetail({ session, onBack }) {
  const m = MODULE_CONFIG[session.moduleId]
  const taps = session.taps ?? []
  const hitTimes = taps.filter(t => t.correct && t.time != null).map(t => t.time)
  const maxTime  = Math.max(...hitTimes, 1)

  const fmtDate = (iso) => new Date(iso).toLocaleString('de-DE', {
    day: '2-digit', month: '2-digit', hour: '2-digit', minute: '2-digit'
  })

  return (
    <div className={s.root}>
      <div className={s.hd}>
        <button className={s.back} onClick={onBack}>‹</button>
        <div>
          <div className={s.ey}>{m.name} · Tiefenanalyse</div>
          <div className={s.tt}>{fmtDate(session.startedAt)} · {session.variant}</div>
        </div>
      </div>

      <div className={s.hero}>
        <div className={s.hCol}>
          <div className={s.hVal} style={{ color: 'var(--primary)' }}>{session.mainMetric}{m.mainMetricUnit}</div>
          <div className={s.hLbl}>{m.mainMetricLabel}</div>
        </div>
        <div className={s.hCol} style={{ borderLeft: '1px solid var(--border)' }}>
          <div className={s.hVal} style={{ color: 'var(--emerald)' }}>
            {session.score.correct ?? session.score.correctRounds ?? '—'}
          </div>
          <div className={s.hLbl}>Korrekt</div>
        </div>
        <div className={s.hCol} style={{ borderLeft: '1px solid var(--border)' }}>
          <div className={s.hVal}>{session.duration}s</div>
          <div className={s.hLbl}>Dauer</div>
        </div>
      </div>

      {hitTimes.length > 0 && (
        <div className={s.section}>
          <div className={s.secLabel}>Reaktionszeit pro Tipp</div>
          <div className={s.tapBars}>
            {taps.map((tap, i) => {
              const t = tap.time ?? 0
              const h = Math.max(8, Math.min(100, (t / maxTime) * 100))
              const color = !tap.correct ? 'var(--rose)'
                : t < maxTime * 0.4 ? 'var(--emerald)'
                : 'rgba(139,92,246,0.5)'
              return <div key={i} className={s.tapBar} style={{ height: `${h}%`, background: color }} />
            })}
          </div>
          <div className={s.legend}>
            <div className={s.legendItem}><div className={s.legendDot} style={{ background: 'var(--emerald)' }} />Schnell</div>
            <div className={s.legendItem}><div className={s.legendDot} style={{ background: 'rgba(139,92,246,0.5)' }} />Normal</div>
            <div className={s.legendItem}><div className={s.legendDot} style={{ background: 'var(--rose)' }} />Fehler</div>
          </div>
        </div>
      )}

      {taps.length > 0 && (
        <div className={s.section}>
          <div className={s.secLabel}>Jeder Tipp einzeln</div>
          {taps.map((tap, i) => (
            <div key={i} className={[s.tapRow, !tap.correct ? s.tapRowError : ''].join(' ')}>
              <div className={s.tapIdx}>{String(tap.target ?? i + 1).padStart(2, '0')}</div>
              <div className={s.tapBarWrap}>
                <div
                  className={s.tapBarInline}
                  style={{
                    width: `${Math.max(4, Math.min(100, ((tap.time ?? 0) / maxTime) * 100))}%`,
                    background: tap.correct ? 'var(--primary)' : 'var(--rose)',
                  }}
                />
              </div>
              <div className={s.tapTime} style={{ color: tap.correct ? 'var(--text-dim)' : 'var(--rose)' }}>
                {tap.time != null ? `${(tap.time / 1000).toFixed(2)}s` : '—'}
              </div>
            </div>
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **9.6 SessionDetail.module.css erstellen**

```css
.root { padding: 0 12px 32px; display: flex; flex-direction: column; gap: 8px; background: var(--bg); min-height: 100%; overflow-y: auto; }
.hd { padding: 16px 2px 8px; display: flex; align-items: center; gap: 10px; }
.back { width: 26px; height: 26px; border-radius: var(--r-sm); background: var(--surface); border: 1px solid var(--border); color: var(--text-dim); font-size: 0.9rem; cursor: pointer; display: flex; align-items: center; justify-content: center; }
.ey { font-size: 0.58rem; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--primary); }
.tt { font-size: 0.82rem; font-weight: 600; color: var(--text); }
.hero { background: rgba(139,92,246,0.07); border: 1px solid rgba(139,92,246,0.14); border-radius: var(--r); padding: 12px; display: flex; }
.hCol { flex: 1; text-align: center; padding: 0 8px; }
.hVal { font-family: 'Orbitron', sans-serif; font-size: 1.15rem; font-weight: 700; }
.hLbl { font-size: 0.58rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.06em; margin-top: 2px; }
.section { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-sm); padding: 10px 12px; }
.secLabel { font-size: 0.6rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 8px; }
.tapBars { display: flex; align-items: flex-end; gap: 2.5px; height: 44px; margin-bottom: 6px; }
.tapBar { flex: 1; border-radius: 2px 2px 0 0; min-width: 3px; }
.legend { display: flex; gap: 12px; }
.legendItem { display: flex; align-items: center; gap: 4px; font-size: 0.58rem; color: var(--text-dim); }
.legendDot { width: 7px; height: 7px; border-radius: 1px; }
.tapRow { display: flex; align-items: center; gap: 8px; padding: 3px 0; border-bottom: 1px solid var(--border); }
.tapRow:last-child { border: none; }
.tapRowError { background: rgba(251,113,133,0.04); border-radius: 4px; }
.tapIdx { font-family: 'Orbitron', sans-serif; font-size: 0.58rem; font-weight: 700; color: var(--text-dim); width: 22px; }
.tapBarWrap { flex: 1; height: 4px; background: rgba(255,255,255,0.05); border-radius: 2px; overflow: hidden; }
.tapBarInline { height: 100%; border-radius: 2px; }
.tapTime { font-size: 0.6rem; font-family: 'Orbitron', sans-serif; font-weight: 600; width: 32px; text-align: right; }
```

- [ ] **9.7 TabKognitiv.jsx: alle Dashboard-Screens einbinden**

```jsx
import Dashboard     from './Dashboard'
import ModuleDetail  from './ModuleDetail'
import SessionDetail from './SessionDetail'

// Dashboard-Screen (ersetzt Placeholder):
if (nav?.screen === 'module-detail') {
  return <ModuleDetail
    moduleId={nav.moduleId}
    onBack={goBack}
    onSelectSession={(session) => setNav({ screen: 'session-detail', session })}
  />
}
if (nav?.screen === 'session-detail') {
  return <SessionDetail session={nav.session} onBack={goBack} />
}

// Im Tab-Content (Dashboard):
{tab === 'dashboard' && (
  <Dashboard onSelectModule={(id) => setNav({ screen: 'module-detail', moduleId: id })} />
)}
```

- [ ] **9.8 Testen** — Nach mehreren Sessions: Dashboard zeigt Sessions-Count, Modul-Kacheln mit Charts. Klick auf Kachel → ModuleDetail. Klick auf Session → SessionDetail mit Tipp-Zeiten.

- [ ] **9.9 Commit**

```bash
git add src/features/tools/kognitiv/
git commit -m "feat(kognitiv): Dashboard, ModuleDetail, SessionDetail — volle Analyse-Chain"
```

---

## Task 10: Kalender-Dot + Abschluss

**Files:**
- Modify: `src/features/calendar/TabKalender/TabKalender.jsx`

- [ ] **10.1 Kalender-Dot für kognitiv**

In `TabKalender.jsx` in der Dot-Berechnung ergänzen (nach dem haushalt-Dot, im `getDots`-Pattern):

```js
import { loadSessions as loadKognitivSessions } from '../../tools/kognitiv/sessionStore'

// In getDots(dk) oder dort wo Tool-Dots berechnet werden:
const kognitivSessions = loadKognitivSessions()
const hasKognitivToday = kognitivSessions.some(s => s.date === dk)
// hasKognitivToday → Dot mit getToolColor('kognitiv', toolColors)
```

Genaue Stelle: Suche im TabKalender.jsx nach `haushalt` Dot-Logik und ergänze analog. Das Pattern ist immer `todos.some(...)` oder `sessions.some(s => s.date === dk)`.

- [ ] **10.2 End-to-End testen**

Checklist:
- [ ] Alle 3 Module komplett durchspielbar (Briefing → Übung → Results)
- [ ] Results: Kalender speichern → Slot erscheint im Tagesplaner
- [ ] Dashboard: Sessions sichtbar nach Absolvieren
- [ ] ModuleDetail: Trend-Chart + Session-Liste korrekt
- [ ] SessionDetail: Tipp-Zeiten-Balken sichtbar
- [ ] DoneToday erscheint wenn Modul heute schon gemacht
- [ ] Alertness Mit-Warnton: Ton hörbar vor Stimulus
- [ ] Zahlensuche Rückwärts: 25→01
- [ ] Zahlensuche Schwer: 6×5 Grid mit 01–30
- [ ] Gedächtnis: 8 Runden (2,2,3,3,4,4,5,5), Kreise als Ring

- [ ] **10.3 kontext-queue.md aktualisieren**

```
kognitiv: neues Tool (Tab 16) — sessionStore, 3 Module (alertness/zahlensuche/gedaechtnis), Dashboard+Archiv, Kalender-Integration
```

- [ ] **10.4 Final commit**

```bash
git add .
git commit -m "feat(kognitiv): complete — 3 Module, Dashboard, Tiefenanalyse, Kalender-Dot"
```
